{
  "name": "whatsapp-ai-real-estate-agent",
  "version": "1.0.0",
  "description": "WhatsApp AI Real Estate Agent - Root Project",
  "scripts": {
    "start": "npm run dev",
    "dev": "npm run db:test && concurrently \"npm start --prefix backend\" \"npm run dev --prefix frontend\"",
    "db:test": "node backend/src/db/testConnection.js",
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "pull:all": "git pull && npm run install:all"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
